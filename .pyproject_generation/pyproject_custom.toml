[project]
name = "top"
version = "2.2.0"
description = "Test OpenID Connect provider"
dependencies = [
    "typer >= 0.15",
    "ghga-service-commons[api,auth] >= 4.1, < 5",
    "hexkit >= 5, < 6",
    "python-multipart >= 0.0.20"
]

[project.urls]
Repository = "https://github.com/ghga-de/test-oidc-provider"

[project.scripts]
top = "top.__main__:run"
